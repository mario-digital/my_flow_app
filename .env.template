# MyFlow Environment Variables Template
#
# This file documents all required environment variables for MyFlow.
# Secrets are stored in 1Password and injected via `op run` command.
#
# Usage:
#   op run --env-file=.env.template -- bun run dev
#
# Note: Never commit actual .env files to Git!

# ============================================================================
# FRONTEND ENVIRONMENT VARIABLES (my_flow_client/)
# ============================================================================

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Logto Authentication (Frontend)
# Get these values from 1Password vault "my_flow_secrets" > "myflow_logto_frontend"
NEXT_PUBLIC_LOGTO_ENDPOINT=op://my_flow_secrets/myflow_logto_frontend/NEXT_PUBLIC_LOGTO_ENDPOINT
NEXT_PUBLIC_LOGTO_APP_ID=op://my_flow_secrets/myflow_logto_frontend/NEXT_PUBLIC_LOGTO_APP_ID
NEXT_PUBLIC_LOGTO_RESOURCE=op://my_flow_secrets/myflow_logto_frontend/NEXT_PUBLIC_LOGTO_RESOURCE
LOGTO_APP_SECRET=op://my_flow_secrets/myflow_logto_frontend/LOGTO_APP_SECRET
LOGTO_COOKIE_SECRET=op://my_flow_secrets/myflow_logto_frontend/LOGTO_COOKIE_SECRET

# ============================================================================
# BACKEND ENVIRONMENT VARIABLES (my_flow_api/)
# ============================================================================

# Database Configuration
# MongoDB Atlas connection string from 1Password
MONGODB_URI=op://my_flow_secrets/myflow_mongodb/MONGODB_URI

# Logto Authentication (Backend)
# Backend uses same Logto credentials as frontend for JWT validation
# Get these values from 1Password vault "my_flow_secrets" > "myflow_logto_frontend"
LOGTO_ENDPOINT=op://my_flow_secrets/myflow_logto_frontend/NEXT_PUBLIC_LOGTO_ENDPOINT
LOGTO_APP_ID=op://my_flow_secrets/myflow_logto_frontend/NEXT_PUBLIC_LOGTO_APP_ID
LOGTO_APP_SECRET=op://my_flow_secrets/myflow_logto_frontend/LOGTO_APP_SECRET
LOGTO_RESOURCE=
LOGTO_JWKS_TIMEOUT_SECONDS=5
LOGTO_JWKS_CACHE_TTL_SECONDS=3600

# CORS Configuration
CORS_ORIGINS=["http://localhost:3000"]

# AI Provider API Keys
# Note: Update these references if AI keys are stored in my_flow_secrets vault
AI_PROVIDER=openai
AI_MODEL=gpt-4o
OPENAI_API_KEY=op://my_flow_secrets/open_ai_api/OPENAI_API_KEY
# ANTHROPIC_API_KEY=  # Optional: Add op://my_flow_secrets/anthropic/ANTHROPIC_API_KEY if needed