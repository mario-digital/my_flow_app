# MyFlow API Environment Configuration
# This file uses 1Password CLI references for secret injection
# Run with: op run --env-file=.env.template -- uvicorn src.main:app --reload

# MongoDB Configuration
MONGODB_URI=op://my_flow_secrets/myflow_mongodb/MONGODB_URI
MONGODB_DB_NAME=op://my_flow_secrets/myflow_mongodb/MONGODB_DB_NAME

# Application Configuration
ENV=development
PROJECT_NAME=MyFlow API
VERSION=0.1.0
API_V1_STR=/api/v1

# CORS Configuration
CORS_ORIGINS=["http://localhost:3000"]

# Logto Authentication Configuration (from Story 1.2 - set to empty if not configured)
LOGTO_ENDPOINT=
LOGTO_APP_ID=
LOGTO_APP_SECRET=

# AI Configuration (for future stories - set to empty for now)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
